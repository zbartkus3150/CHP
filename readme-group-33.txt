The code is written in python so to run it, after extracting source file from the zip file, one should execute the command "python project.py < FILENAME"
Where FILENAME is the full name of the .SWE file
Example:
"python project.py < test01.swe"